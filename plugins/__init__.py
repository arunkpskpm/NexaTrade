"""
NexaTrade — Strategy Plugins Directory.

Drop your strategy .py files here.
The StrategyEngine will auto-discover them on startup.

Each file must contain at least one class that:
    - Inherits from AbstractStrategy
    - Sets a unique STRATEGY_NAME class attribute
    - Implements all abstract methods

See plugins/ema_crossover.py for a complete example.
"""